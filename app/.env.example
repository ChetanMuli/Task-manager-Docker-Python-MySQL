# Example environment variables for MySQL
# Option A: provide full SQLAlchemy URI
# DATABASE_URL="mysql+pymysql://taskuser:Pass@123@localhost:3306/taskdb?charset=utf8mb4"

# Option B: provide components and the app will build the URI
DB_USER=taskuser
DB_PASS=Pass@123
DB_HOST=localhost
DB_PORT=3306
DB_NAME=taskdb

# Flask settings
FLASK_APP=app.py
SECRET_KEY=change_this_secret_for_prod
